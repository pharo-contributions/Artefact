Class {
	#name : #PDFLineFeed,
	#superclass : #PDFLayout,
	#category : #Artefact
}

{ #category : #render }
PDFLineFeed >> renderContentOn: aDocument [
	^''
]

{ #category : #render }
PDFLineFeed >> renderContentOn: aPage of: aDocument [
	^''
]
